# 图论

## 定义

- 图(Graph)是由顶点的有穷非空集合V(G)和顶点之间边的集合E(G)组成。
- 通常表示为: G=(V,E)，其中，G表示图，V是图G中顶点的集合，E是图G中边的集合。
- 若V={v1,v2,...,vn }，则用∣V∣表示图G中顶点的个数，也称图G的阶，E={(u,v)∣u∈V,v∈V}，用∣E∣表示图G中边的条数。

## 有向图与无向图

- 若E是有向边(也称弧)的有限集合时，则图G为**有向图**。弧是顶点的**有序对**，记为<v, w>，其中v,w是顶点，v称为弧尾，w称为弧头，<v,w>称为从顶点v到顶点w的弧，也称v邻接到w，或w邻接自v。
- 若E是无向边(简称边)的有限集合时，则图G为**无向图**。边是顶点的**无序对**，记为(v, w)或(w,v),因为(v,w)=(w,v), 其中v,w是顶点。可以说顶点w和顶点v互为邻接点。边(v, w)依附于顶点w和v，或者说边(v, w)和顶点v, w相关联。

## 加权图

- 定义：每条边都被赋予一个权值（数值），代表距离、成本、容量等含义。

## 邻接矩阵

- 定义：用二维数组（矩阵）存储图，行和列都对应顶点。
- 无向图：
  A [i][j]= 1 表示顶点 i 和 j 之间有边，0 表示无边。 矩阵是对称矩阵（A[i][j] = A[j][i]），对角线 A[i][i] = 0（无自环）。
- 有向图：
  A[i][j] = 1 表示存在从 i 到 j 的弧，0 表示无弧。
  矩阵不一定对称，行 i 的和是顶点 i 的出度，列 j 的和是顶点 j 的入度。
- 加权图：
  A[i][j] 存储边的权值，无边时用 ∞（无穷大）或 0 表示。
  例：图中 v₁ 行的和为 1+0+1+0 = 2，即 v₁ 的度为 2（无向图中，度是连接的边数）。



# 多智能体系统一致性

## 定义

- 多智能体系统一致性算法是指在一个由多个智能体组成的系统中，令所有智能体的某特征值达成一致的算法。这种算法可以应用于分布式控制、协同感知、集群智能等领域，是多智能体系统中的一项核心技术。

## 公式

- | 符号  | 含义                                                         |
  | ----- | ------------------------------------------------------------ |
  | xi    | 第 i 个智能体（可以理解为网络中的一个节点 / 个体）           |
  | N     | 智能体的总数量（网络的节点总数）                             |
  | aij   | 邻接矩阵元素：aij=1 表示**第 i 个智能体和第 j 个智能体直接相连**（是邻居）；aij=0 表示两者不相连 |
  | T     | 算法的总迭代轮数                                             |
  | wij   | 加权系数：第 i 个智能体对第 j 个智能体状态值的权重           |
  | xi(t) | 第 i 个智能体在**第 t 轮迭代**时的状态值（比如传感器的测量值、智能体的决策值等） |

- ![image-20260321212545906](C:\Users\30346\AppData\Roaming\Typora\typora-user-images\image-20260321212545906.png)   ![image-20260321212613932](C:\Users\30346\AppData\Roaming\Typora\typora-user-images\image-20260321212613932.png)



# PPT思考题

- 1.有向 / 有权图参数变化：核心是邻接矩阵a ij从 0-1 变为非负实值，度从无向的入度 = 出度变为有向的出度 / 入度分离，权重矩阵仍需满足行随机约束。

- 2.矩阵形式：

  ![image-20260321213334619](C:\Users\30346\AppData\Roaming\Typora\typora-user-images\image-20260321213334619.png)

  矩阵形式；

  ![image-20260321213409840](C:\Users\30346\AppData\Roaming\Typora\typora-user-images\image-20260321213409840.png)

  

- 3.一致值与收敛性：无向连通图收敛到初始平均值；有向强连通非周期图收敛到加权平均值；图的连通性、周期性、权重性质是收敛的核心影响因素。

- 4.离散 / 连续对比：目标一致，实现方式不同，离散适合数字系统，连续适合物理系统，离散可视为连续的欧拉离散化。